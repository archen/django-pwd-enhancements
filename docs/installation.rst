============
Installation
============

At the command line::

    $ easy_install django-pwd-enhancements

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pwd-enhancements
    $ pip install django-pwd-enhancements